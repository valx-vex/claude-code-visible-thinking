{
  "bindings": [
    {
      "context": "Global",
      "bindings": {
        "ctrl+shift+o": "app:toggleTranscript"
      }
    },
    {
      "context": "Chat",
      "bindings": {
        "ctrl+shift+t": "chat:thinkingToggle"
      }
    }
  ]
}
